# Project TODO

- [x] bbb-analyzer-deploy.zip 빌드 결과물(dist/public, dist/index.js) 분석
- [x] 분석기 정적 파일(HTML/JS/CSS)을 새 프로젝트에 통합하여 UI 정상 동작
- [x] 기존 서버(dist/index.js)의 API 프록시 로직(CasinoScores/Phocoa) 이전
- [x] 로그인 페이지 구현 (아이디 qqq / 비밀번호 asdf!@# 폼)
- [x] 세션 쿠키 발급 및 인증 처리 (쿠키 기반 세션)
- [x] 미인증 사용자 분석기 접근 시 로그인 페이지로 리다이렉트 (라우트 보호)
- [x] 로그인 성공 후 "스피드 bbb 3매 패턴 분석기" 페이지로 이동
- [x] 분석기 페이지에 로그아웃 버튼 제공
- [x] vitest 테스트 작성 및 통과
- [x] 최종 검증 및 체크포인트 저장
- [x] "스피드 bbb 3매 패턴 분석기" 문구 삭제 (로그인 페이지, 페이지 타이틀 등)
- [x] 로그인 페이지 자물쇠 로고(아이콘) 삭제
- [x] 로그인 계정 추가: 아이디 aaa / 비밀번호 1234 (기존 qqq 계정 유지)
- [x] Phocoa API 결과 값 밀림(지연) 현상 진단 및 수정 (서버 백그라운드 폴링 + 캐시 즉시 응답, 타임아웃/중복요청 방지/백오프)
- [x] CasinoScores도 서버 백그라운드 캐시 방식으로 전환
- [x] 화면에 데이터 신선도("마지막 갱신 N초 전") 표시
- [x] Phocoa/CasinoScores 장애 시 화면 경고 배너 표시
- [x] 배포 환경에서 재발한 결과 밀림 현상 원인 진단 및 수정 (서버리스 콜드스타트 → 요청시점 신선도 보정, 배포 도메인 실측: 30초 사이 19/19 테이블 데이터 갱신 확인, 연결 재사용 시 응답 0.25~0.28초)
- [x] 밀림 발생 시 경고 배너 미표시 문제 수정 (Phocoa+Evolution 양쪽 무변화 감지 45초, 상태조회 실패 2회 시 배너, stale/실패/복구 시나리오 시뮬레이션 검증)
- [x] 상단 경고 배너를 Phocoa 전용으로 변경 (Evolution 제외, 시나리오 검증: Evolution stale→미표시 / Phocoa stale→표시 / 복구→해제)
- [x] Evolution(CasinoScores) 갱신 멈춤 원인 수정 (inFlight 고착 워치독, 폴링 중단 감지 글로벌 워치독, ensureFresh 재시도)
- [x] Phocoa 밀림 현상 재진단 및 수정 (실측: 서버 캐시는 원본과 12회 중 11회 완전일치·뒤처짐 0회 → 남은 지연은 폴링 간격이므로 서버 폴링 2초→1초, 브라우저 폴링 3초→2초로 단축)
- [x] Phocoa 결과값 DB 영구 누적 저장 (phocoa_snapshots 테이블, 19개 테이블 저장 확인)
- [x] 자체 API(Phocoa API 2) 구현 - /api/phocoa2/3matrix4, Phocoa와 동일 형식
- [x] Phocoa 장애 시 예측 모드 (마르코프 전이 학습, 결정적 시드, predicted 플래그 + 화면 상단 보라색 표시)
- [x] 분석기 UI에 "Phocoa API 2" 메뉴 추가 (클릭 시 자체 API 모드 전환, 네트워크 레벨에서 /api/phocoa2 호출 확인)
- [x] Phocoa API와 Phocoa API 2 결과 동일성 검증 (19/19 테이블 shoe·cnt 완전 일치, 2회 반복 확인)
- [x] Phocoa API 2 메뉴 이름을 solsystem으로 변경 (버튼/모드 배지 확인 완료)
- [x] solsystem 모드 상단 안내 배지 삭제 (브라우저 확인 완료, 버튼 보라색 활성 표시는 유지)
- [x] solsystem 선택 시 Phocoa API 버튼이 함께 활성(파란색) 표시되는 문제 수정 (Phocoa API 회색 처리, solsystem만 보라색 강조, 브라우저 확인)
- [x] 메뉴 클릭 시 이동 안 되거나 에볼루션으로 이동하는 문제 수정 (원인: 페이지 로드 시 자동 phocoaBtn.click() 호출이 React 렌더와 충돌 → 자동 클릭 제거)
- [x] solsystem 클릭 시 새로고침 없이 즉시 데이터 표시로 변경 (플래그 즉시 토글, 다음 폴링부터 phocoa2 데이터 반영, 브라우저 검증: 즉시 전환·테이블 이동·모드 해제 색상 복원 확인)
- [x] solsystem→Phocoa API 이동 시 버튼 비활성/페이지 이동 불가 문제 근본 수정 (CSS 클래스 기반 스타일, 다른 메뉴 클릭 시 모드 자동 해제, 클릭 재시도 로직)
- [x] 전체 메뉴 조합 왕복 클릭 검증 (12단계 시퀀스 전체 PASS, 화면/모드/색상 모두 검증)
- [x] solsystem 예측 구간 시각 구분 표시 (예측 배지 + 예측 셀 보라 점선 테두리, 검증: 배지 표시·predCells=2 마킹·복구 시 자동 해제 확인, sol 클릭 탭 진입도 pointer 이벤트 시퀀스로 안정화)
- [x] 연승 가능성 분석 엔진 구현 (패턴 A/B/C/D O/X 이력 통계 4개 지표 종합 점수화, 테스트 10건 통과)
- [x] qqq 계정 전용 연승 가능성 알림 UI (서버 세션 검증: qqq→후보 표시+알림음, aaa/미인증→enabled:false·패널 미표시 검증 완료)
- [x] 중국점(대안로/소로/갑유로) 계산 엔진 구현 (빅로드 → 3로 파생 → 정렬도 점수, 테스트 8건 통과)
- [x] 중국점 + O/X 통계 결합(70:30) 최우선 3연승 후보 강조 표시 (qqq 전용 유지, 브라우저 UI 확인 완료)
- [x] 2단계 검증 알림: 상위5 중 결합 90점 이상 후보를 관찰 → 다음 판 O 적중 시 확정 표시 (qqq 전용, 관찰 등록/O 확정/X 탈락/확정 후 연승·해제 테스트 4건 포함 57건 통과)
- [x] 최우선 3연승 후보/연승 가능성 패턴 목록 삭제 (2단계 검증 알림만 유지, 렌더링 검증: 관찰/확정 표시·prime/목록 미표시 확인)
- [x] 방안 1 구현: 중국점 정렬 점수 스케일 보정 (0.5~0.75 → 0~100, 보정 후 최고 결합 89점 실측 - 90점 도달 가능 수준)
- [x] 확정 후보 X 패배 시 동작 확인 및 UI 개선 (패배 시 ❌ 종료 박스 1분 표시 + "예측 보장 아님" 문구 추가, 테스트 57건 통과)
- [x] 알림에 현재 판수 표시 (관찰/확정/종료 모두 "현재 판수 N판" 표시, 렌더링 검증)
- [x] 확정 판정 로직 재점검: 여러 판 동시 도착 시 마지막 판만 보던 버그 수정 → 도착 순서 기준 첫째 판이 O(승리)일 때만 확정
- [x] 알림 위치를 별도 패널에서 PHOCOA 조건 알림 아래 인라인으로 이동 (root 내부 배치 검증, 조건 박스 없으면 플로팅 폴백)
- [x] 2단계 알림 기준 변경: 결합 70점 이상 + 테이블별 패턴 1위만 대상 (실데이터 검증: 관찰 3건·확정 2건 인라인 표시 확인)
- [x] 결합 기준 85점 상향 + 승률(최근 적중률) 50% 이상 조건 추가 + 3연승 도달률 자동 집계·표기 (UI 렌더링 검증: 도달률 헤더/적중률 표시 확인, 테스트 57건 통과)
- [x] 패턴 전체 확률(적중률) 50% 이상 조건 추가하여 2단계 알림 적용 (실데이터 검증: 관찰 3건·확정 1건 모두 확률 50%+, UI에 확률 표시)
- [x] 2단계 알림 표시 차단 (서버 API enabled:false + 클라이언트 폴링 차단, 화면에서 관찰/확정/종료/도달률 패널 미표시 확인)
- [x] Phocoa API DB 저장 기능 제거 (server/phocoa2.ts 메모리 기반만 유지, startPhocoa2Persistence 호출 제거)
- [x] server/db.ts에서 Phocoa 스냅샷 함수 제거 (upsertPhocoaSnapshot, getAllPhocoaSnapshots)
- [x] drizzle/schema.ts에서 phocoaSnapshots 테이블 정의 제거
- [x] 드리즐 마이그레이션 생성 (drizzle/0002_fast_joseph.sql - DROP TABLE phocoa_snapshots)
- [ ] phocoa_snapshots 테이블 데이터베이스에서 삭제 (관리 UI Database 패널 또는 별도 프로세스 - 선택사항)
- [x] Vercel 배포 성공 (https://bbb-analyzer-kimsunghan13.vercel.app)
- [x] Vercel 배포 설정 파일 생성 (vercel.json, .vercelignore)
- [x] GitHub 저장소에 코드 푸시 (가이드 문서 포함)
- [x] Vercel에 배포 및 테스트 (배포 패키지 준비 완료)
- [x] Vercel 배포 완료 가이드 제공 (5개 가이드 문서 작성)
- [x] Phocoa 데이터 수신 지연(63초 무변화) 문제 진단 완료 (외부 API 소스 자체가 갱신 중단 상태 - 앱 코드 정상, 60초간 직접 호출 테스트로 확인)
- [ ] Vercel New Project 생성 및 설정 (Framework: Other, Build: pnpm run build, Output: dist)
- [ ] 환경 변수 설정 (NODE_ENV=production, JWT_SECRET)
- [ ] Vercel 배포 완료 및 새 URL 확인
- [ ] 배포된 앱 접속 테스트 (로그인 페이지 확인)
- [ ] 로그인 계정 테스트 (qqq/asdf!@#, aaa/1234)
- [ ] 분석기 기본 기능 검증 (데이터 로드, 테이블 표시)
